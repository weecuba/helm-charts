apiVersion: v1
kind: Service
metadata:
  name: {{ include "weecuba-dotnet-webapi.fullname" . }}
  labels:
    {{- include "weecuba-dotnet-webapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weecuba-dotnet-webapi.selectorLabels" . | nindent 4 }}
