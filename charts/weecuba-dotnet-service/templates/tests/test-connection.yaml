apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weecuba-dotnet-service.fullname" . }}-test-connection"
  labels:
    {{- include "weecuba-dotnet-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weecuba-dotnet-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
